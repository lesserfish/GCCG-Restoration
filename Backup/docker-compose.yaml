version: "3"

services:
  backup:
    image: ubuntu:22.04
    container_name: gccg_backup
    command: /bin/bash -c /tmp/backup.sh
    restart: "no"
    volumes:
      - ../mtg/:/mnt/data
      - ./contents/backup.sh:/tmp/backup.sh
      - ./contents/.env:/tmp/.env
